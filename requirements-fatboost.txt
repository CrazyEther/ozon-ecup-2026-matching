numpy>=1.26,<3
pandas>=2.1,<3
pyarrow>=14,<20
scikit-learn>=1.4,<2
rapidfuzz>=3.9,<4
catboost>=1.2.8,<2
joblib>=1.3,<2

# Only needed when --embedding-model is used. The zero-neural edge profile
# (rules + TF-IDF + native CatBoost text) does not import these packages.
torch>=2.4,<3
transformers>=4.45,<5
